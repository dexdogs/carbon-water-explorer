# Mapbox public token — get yours at https://account.mapbox.com/access-tokens/
# The NEXT_PUBLIC_ prefix exposes this to the browser (required for Mapbox GL)
NEXT_PUBLIC_MAPBOX_TOKEN=pk.your_token_here
